name: Build Linode Image

on:
  # Manual trigger
  workflow_dispatch:
  # Build ALL trigger
  workflow_dispatch:

jobs:
  build:
    name: Build Linode Image
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4